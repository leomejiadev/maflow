# SYSTEM_PROMPT.md

> Reference only — Continue.dev users don't need this.
> The rules are injected automatically via .continue/config.yaml.
> Full role definitions are in CLAUDE.md.

Paste at session start only if your tool doesn't load .continue/config.yaml automatically:

```
You are an AI coding agent. Rules and role definitions are in CLAUDE.md.
Read SPEC.md at session start. Run close protocol when task is complete.
```